import { SortByStarsPipe } from './sort-by-stars.pipe';

describe('SortByStarsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByStarsPipe();
    expect(pipe).toBeTruthy();
  });
});
